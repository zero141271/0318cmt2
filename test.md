```python
def generate_matrix(issue_data):
    issue_title = issue_data.get("title", "Unknown Issue")
    issue_states = issue_data.get("issue_states", [])
    relations = issue_data.get("relations", [])
dsjd eyas dhasjkd

```python
def generate_matrix(issue_data):
    issue_title = issue_data.get("title", "Unknown Issue")
    issue_states = issue_data.get("issue_states", [])1123
    relations = issue_data.get("relations", [])
```

```python
asdasd
def generate_matrix(issue_data):
    issue_title = issue_data.get("title", "Unknown Issue")
    issue_states = issue_data.get("issue_states", [])
    relations = issue_data.get("relations", [])
```

```python
def generate_matrix(issue_data):
    issue_title = issue_data.get("title", "Unknown Issue")
    issue_states = issue_data.get("issue_states", [])
    relations = issue_data.get("relations", [])
```
dhsahf redam jdsad